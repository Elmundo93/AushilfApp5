import React from 'react';
import { View, Text, Button } from 'react-native';



const einstellungen  = () => {
  
 

  return (
    <View>
      <Text></Text>
    
      <Button
        title='Go to Details'
        onPress={() => void(0)}
      />
    </View>
  );
};

export default einstellungen;